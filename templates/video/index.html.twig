{% extends 'base.html.twig' %}

{% block content %}

    {% block title %}Edit Video{% endblock %}

    {% block body %}

        {{ form_start(form) }}

        <div class="embed-responsive embed-responsive-4by3">
            <iframe width="100%" height="500px" class="embed-responsive-item" src="{{ video.videoUrl }}"
                    title="YouTube video player"
                    allowfullscreen></iframe>
        </div>
        {{ form_row(form.videoUrl) }}
        <button type="submit" class="btn btn-outline-info">Update video</button>

        {{ form_end(form) }}


    {% endblock %}

{% endblock %}
