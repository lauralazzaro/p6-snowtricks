<form class="float-right" style="display: inline" method="post"
      action="{{ path('app_trick_delete', {'slug': trick.slug, 'token' : csrf_token('delete' ~ trick.id)}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');">
	<button class="btn"><i class="fa-solid fa-trash-can"></i></button>
</form>
