
<form class="float-right" style="display: inline" method="post" action="{{ path('app_trick_delete', {'slug': trick.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trick.slug) }}">
    <button class="btn"><i class="fa-solid fa-trash-can"></i></button>
</form>
